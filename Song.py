import Measure

Class Song:
  'The whole song file'
  
  # Create a list of measures
  measureList = []
  measureCount = 
  
  # Create a measure for each soundbyte
  for x in range(soundBytes):
    x = Measure(soundByte, x)
    measureList.append(x)
